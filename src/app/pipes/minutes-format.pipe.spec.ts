import { MinutesFormatPipe } from './minutes-format.pipe';

describe('MinutesFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutesFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
